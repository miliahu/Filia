events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # تنظیمات لاگ
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;
    error_log   /var/log/nginx/error.log warn;

    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout  65;

    # افزایش سقف حجم بادی برای آپلود فایل (پیش‌فرض Nginx ۱ مگابایت است)
    client_max_body_size 100M;
    client_body_buffer_size 128k;

    # تایم‌اوت‌های مناسب برای فایل‌های سنگین
    proxy_connect_timeout       60s;
    proxy_send_timeout          120s;
    proxy_read_timeout          120s;

    upstream filia_backend {
        server filia-api:8080;
    }

    server {
        listen 80;
        server_name localhost;

        location / {
            proxy_pass http://filia_backend;
            proxy_http_version 1.1;

            # پاس دادن هدرهای کلاینت به ASP.NET Core
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection keep-alive;
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Real-IP $remote_addr;

            # غیرفعال کردن بافر در صورت نیاز به Streaming مستقیم آپلود/دانلود
            proxy_request_buffering off;
            proxy_buffering off;
        }
    }
}
